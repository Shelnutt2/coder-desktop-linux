name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Run weekly on Monday at 06:00 UTC
    - cron: "0 6 * * 1"

permissions:
  security-events: write
  contents: read
  actions: read

env:
  GO_VERSION: "1.25"
  WAYLAND_VERSION: "1.23.1"
  WLROOTS_VERSION: "0.19.0"

jobs:
  analyze-cpp:
    name: Analyze C/C++
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: coder-vpn-linux/go.sum

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            qt6-base-dev \
            qt6-declarative-dev \
            qt6-tools-dev-tools \
            libqt6quick3d6 \
            libqt6websockets6-dev \
            qt6-webengine-dev \
            libsecret-1-dev \
            wayland-protocols \
            pkg-config \
            bubblewrap \
            ninja-build \
            meson \
            libffi-dev \
            libxml2-dev \
            libdrm-dev \
            libgbm-dev \
            libxkbcommon-dev \
            libegl-dev \
            libgles-dev \
            libpixman-1-dev \
            libseat-dev \
            hwdata \
            libdisplay-info-dev \
            libliftoff-dev \
            libxcb-dri3-dev \
            libxcb-present-dev \
            libxcb-composite0-dev \
            libxcb-render-util0-dev \
            libxcb-ewmh-dev \
            libxcb-xinput-dev \
            libxcb-icccm4-dev \
            libxcb-res0-dev

      - name: Build wayland ${{ env.WAYLAND_VERSION }} from source
        run: |
          git clone --depth 1 --branch ${{ env.WAYLAND_VERSION }} https://gitlab.freedesktop.org/wayland/wayland.git /tmp/wayland
          cd /tmp/wayland
          meson setup build --prefix=/usr --buildtype=release -Ddocumentation=false -Dtests=false
          ninja -C build
          sudo ninja -C build install
          sudo ldconfig

      - name: Build wlroots ${{ env.WLROOTS_VERSION }} from source
        run: |
          git clone --depth 1 --branch ${{ env.WLROOTS_VERSION }} https://gitlab.freedesktop.org/wlroots/wlroots.git /tmp/wlroots
          cd /tmp/wlroots
          meson setup build --prefix=/usr --buildtype=release -Dxwayland=disabled --wrap-mode=default
          ninja -C build
          sudo ninja -C build install
          sudo ldconfig

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: c-cpp

      - name: CMake configure
        run: |
          cmake -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: cmake --build build -j$(nproc)

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:c-cpp"

  analyze-go:
    name: Analyze Go
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: coder-vpn-linux/go.sum

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: go

      - name: Autobuild Go
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:go"
